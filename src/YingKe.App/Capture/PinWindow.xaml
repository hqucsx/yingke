<Window x:Class="YingKe.App.Capture.PinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="映刻 钉图"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        ShowActivated="False"
        ContextMenuOpening="OnContextMenuOpening"
        FontFamily="Microsoft YaHei UI">
    <Window.Resources>
        <Style x:Key="PinMenu" TargetType="MenuItem">
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Border x:Name="ShadowBorder" Margin="10" CornerRadius="2"
                Background="#01000000">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="14" ShadowDepth="4" Opacity="0.55"/>
            </Border.Effect>
            <Image x:Name="PinImage" Stretch="Fill"/>
        </Border>
    </Grid>
</Window>
